apiVersion: v1
kind: Service
metadata:
  name: employee-management
  labels:
    app: employee-management
    version: v1

spec:
  type: ClusterIP
  selector:
    app: employee-management
  ports:
    - name: http
      port: 80
      targetPort: 8080