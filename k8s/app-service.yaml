apiVersion: v1
kind: Service
metadata:
  name: employee-management
spec:
  selector:
    app: employee-management
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
    - type: NodePort